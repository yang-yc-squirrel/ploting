import matplotlib.pyplot
import numpy
import pandas
import random
import functools
import itertools
import json
import os

a=numpy.array([random.choice(list(range(3))) for i in range(15)])
print(a)
b=list(range(10))
print(b)